name: Publish CLI

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check out the public website Skill mirror
        uses: actions/checkout@v6
        with:
          repository: wegoft/exportdou
          path: exportdou-site
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: npm ci
      - run: npm test
        env:
          EXPORTDOU_PUBLIC_SKILL_PATH: ${{ github.workspace }}/exportdou-site/packages/web/public/SKILL.md
      - name: Publish when this version is not already on npm
        shell: bash
        run: |
          version=$(node -p "require('./packages/cli/package.json').version")
          if npm view "exportdou@${version}" version >/dev/null 2>&1; then
            echo "exportdou@${version} is already published"
          else
            npm publish --workspace exportdou --access public
          fi
